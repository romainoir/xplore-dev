# Vendor

This folder contains source or reference code imported from external projects
that is still useful to the app.

- `maplibre/`: local MapLibre source fork used to build `scripts/map/maplibre-gl-dev.js`.
