# Archive

This folder keeps non-runtime references, legacy experiments, and older local
copies that should not sit at the repository root.

Nothing here is required by the main `index.html` runtime unless a file says so
explicitly.
